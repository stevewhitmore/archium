{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "../out-tsc/app",
    "types": ["node"],
    "baseUrl": ".",
    "paths": { 
        "@enums": ["./app/_enums/*"] 
    }
  },
  "exclude": [
    "src/test.ts",
    "**/*.spec.ts"
  ]
}
